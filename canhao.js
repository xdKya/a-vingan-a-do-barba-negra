class Canhao {
  constructor(x, y, w, h, angle) {
    //propriedades
    this.x = x;
    this.y = y;
    this.w = w;
    this.h = h;
    this.angle = angle;
  }

  //funções
  show() {
    //base do canhao
    //topo do canhao
  }
}
